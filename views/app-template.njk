{% extends "hmpo-template.njk" %}

{% from "hmpo-footer/macro.njk" import hmpoFooter %}
{% from "hmpo-html/macro.njk" import hmpoHtml %}

{% block pageTitle %}
    {{ (translate("govuk.error", { default: "Error" }) + ": ") if errorlist.length }}{{ govukServiceName | safe if govukServiceName }} – GOV.UK
{% endblock %}

{% block footer %}
    {{ hmpoFooter({
        meta: {
            html: hmpoHtml(translate("footer"))
        }
    }) }}
{% endblock %}
